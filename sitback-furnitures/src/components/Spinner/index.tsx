/*
	@author Aravindhan A
	@description This component is fallback component to be rendered when data is being loaded.
*/

const Spinner = () => {
	return <div>Loading...</div>;
};

export default Spinner;
